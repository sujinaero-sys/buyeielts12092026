# BUYE IELTS Backend â€” Google Apps Script

## Google Sheet
The frontend is prepared for this database:

`YOUR_GOOGLE_SHEET_ID`

## Setup
1. Open the Google Sheet.
2. Extensions â†’ Apps Script.
3. Paste `code.gs`.
4. Run `setup()` once and authorize it.
5. Deploy â†’ New deployment â†’ Web app.
6. Execute as: **Me**.
7. Who has access: **Anyone**.
8. Copy the deployed `/exec` URL.
9. Put that URL into `js/api.js` as `API_URL`.
10. Reload the website.

The website uses `text/plain` fetch requests containing JSON to avoid unnecessary browser preflight handling with Apps Script.

## Tabs created
- Users
- OTPs
- Leads

## Important
The deployed web-app URL is not included in this package because it is generated by your Google Apps Script deployment.

